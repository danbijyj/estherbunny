import styled from 'styled-components';

export const AboutWrap = styled.div`
    .inner {
        padding: 100px 0;
        article {
            display: flex;
            flex-direction: column;
            gap: 50px;
        }
    }
`;
